name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: Existing semantic tag to publish, e.g. v0.1.0
        required: true
        type: string

concurrency:
  group: unetic-web-release-${{ inputs.tag || github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  release:
    uses: Unetic/packages/.github/workflows/reusable-web.yml@main
    with:
      release: true
      ref: ${{ inputs.tag || github.ref_name }}
